export enum gameFBProcessEnum {
  unstart = 0,
  firstHalf = 1,
  internal = 2,
  secondHalf = 3,
  extraTime = 4,
  shootout = 5,
  allowance = 8,
  gameover = 9,
}

export enum footballPlayOrderEnum {
  lineup = 0,
  substitute = 1,
  injury = 2,
  other = 9,
}
